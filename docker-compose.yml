version: "3.9"

services:
  helpdesk:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8100:80"
    #user: "33:33" # user www-data (optional)
    volumes:
      - .:/var/www/
      # - ./docker/app/storage:/var/www/storage
